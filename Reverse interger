class Solution:
    def reverse(self, x: int) -> int:
        original=x
        reverse=0
        while x != 0:
            n=x%10
            x=x//10
            reverse = reverse * 10 + n
        return reverse
            
        
